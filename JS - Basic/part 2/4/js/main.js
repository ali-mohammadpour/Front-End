var show = JSON.parse('{"lang": "JavaScript", "lib": "React.js"}');   // tabdile string be object.

console.log(show.lang);

var newJson = {
    "name": "Ali",
    "age": 30
};

var show2 = JSON.stringify(newJson);   // tabdile object be string.

console.log(show2);
